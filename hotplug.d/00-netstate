[ ifup = "$ACTION" ] && {
        uci_toggle_state network "$INTERFACE" up 1
        [ -n "$DEVICE" ] && {
                uci_toggle_state network "$INTERFACE" ifname "$DEVICE"
        }
}
sh /etc/scripts/net
#echo f | tee /sys/class/net/pppoe-wan/queues/rx-0/rps_cpus
#echo 2048 | tee /sys/class/net/pppoe-wan//queues/rx-0/rps_flow_cnt
